services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: vectorcms
      POSTGRES_PASSWORD: vectorcms
      POSTGRES_DB: vectorcms
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vectorcms"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
